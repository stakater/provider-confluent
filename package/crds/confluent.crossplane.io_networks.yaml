---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: networks.confluent.crossplane.io
spec:
  group: confluent.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - confluent
    kind: Network
    listKind: NetworkList
    plural: networks
    singular: network
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Network is the Schema for the Networks API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NetworkSpec defines the desired state of Network
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicies field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  aws:
                    description: 'The AWS-specific network details if available. It
                      supports the following:'
                    items:
                      type: object
                    type: array
                  azure:
                    description: 'The Azure-specific network details if available.
                      It supports the following:'
                    items:
                      type: object
                    type: array
                  cidr:
                    description: The IPv4 CIDR block to be used for the network. Must
                      be /16. Required for VPC peering and AWS TransitGateway. The
                      IPv4 CIDR block to used for this network. Must be /16. Required
                      for VPC peering and AWS TransitGateway.
                    type: string
                  cloud:
                    description: 'The cloud service provider in which the network
                      exists. Accepted values are: AWS, AZURE, and GCP. The cloud
                      service provider in which the network exists.'
                    type: string
                  connectionTypes:
                    description: 'The list of connection types that may be used with
                      the network. Accepted connection types are: PEERING, TRANSITGATEWAY,
                      and PRIVATELINK.'
                    items:
                      type: string
                    type: array
                  displayName:
                    description: The name of the Network. The name of the Network.
                    type: string
                  dnsConfig:
                    description: 'Network DNS config. It applies only to the PRIVATELINK
                      network connection type. When omitted, resolution defaults to
                      CHASED_PRIVATE. It supports the following: Network DNS config.
                      It applies only to the PRIVATELINK network connection type.'
                    items:
                      properties:
                        resolution:
                          description: Network DNS resolution. When resolution is
                            CHASED_PRIVATE, clusters in this network require both
                            public and private DNS to resolve cluster endpoints. When
                            resolution is PRIVATE, clusters in this network only require
                            private DNS to resolve cluster endpoints. Network DNS
                            resolution.
                          type: string
                      type: object
                    type: array
                  environment:
                    description: 'supports the following: Environment objects represent
                      an isolated namespace for your Confluent resources for organizational
                      purposes.'
                    items:
                      properties:
                        id:
                          description: The ID of the Environment that the Network
                            belongs to, for example, env-abc123. The unique identifier
                            for the environment.
                          type: string
                        idRef:
                          description: Reference to a Environment in confluent to
                            populate id.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: Resolution specifies whether resolution
                                    of this reference is required. The default is
                                    'Required', which means the reconcile will fail
                                    if the reference cannot be resolved. 'Optional'
                                    means this reference will be a no-op if it cannot
                                    be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: Resolve specifies when this reference
                                    should be resolved. The default is 'IfNotPresent',
                                    which will attempt to resolve the reference only
                                    when the corresponding field is not present. Use
                                    'Always' to resolve the reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        idSelector:
                          description: Selector for a Environment in confluent to
                            populate id.
                          properties:
                            matchControllerRef:
                              description: MatchControllerRef ensures an object with
                                the same controller reference as the selecting object
                                is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: Resolution specifies whether resolution
                                    of this reference is required. The default is
                                    'Required', which means the reconcile will fail
                                    if the reference cannot be resolved. 'Optional'
                                    means this reference will be a no-op if it cannot
                                    be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: Resolve specifies when this reference
                                    should be resolved. The default is 'IfNotPresent',
                                    which will attempt to resolve the reference only
                                    when the corresponding field is not present. Use
                                    'Always' to resolve the reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  gcp:
                    description: 'The GCP-specific network details if available. It
                      supports the following:'
                    items:
                      type: object
                    type: array
                  region:
                    description: The cloud provider region where the network exists.
                      The cloud service provider region where the network exists.
                    type: string
                  reservedCidr:
                    description: The reserved IPv4 CIDR block to be used for the network.
                      Must be /24. If not specified, Confluent Cloud Network uses
                      172.20.255.0/24. The IPv4 CIDR block reserved for Confluent
                      Cloud Network. Must be /24. If not specified, Confluent Cloud
                      Network uses 172.20.255.0/24
                    type: string
                  zoneInfo:
                    description: 'Each item represents information related to a single
                      zone. It supports the following: Each item represents information
                      related to a single zone.'
                    items:
                      properties:
                        cidr:
                          description: The IPv4 CIDR block to be used for the network.
                            Must be /16. Required for VPC peering and AWS TransitGateway.
                          type: string
                        zoneId:
                          description: Cloud provider zone ID.
                          type: string
                      type: object
                    type: array
                  zones:
                    description: The 3 availability zones for this network. They can
                      optionally be specified for AWS networks used with PrivateLink,
                      for GCP networks used with Private Service Connect, and for
                      AWS and GCP networks used with Peering. Otherwise, they are
                      automatically chosen by Confluent Cloud. On AWS, zones are AWS
                      AZ IDs, for example, use1-az3. On GCP, zones are GCP zones,
                      for example, us-central1-c. On Azure, zones are Confluent-chosen
                      names (for example, 1, 2, 3) since Azure does not have universal
                      zone identifiers. The 3 availability zones for this network.
                      They can optionally be specified for only AWS networks used
                      with PrivateLink. Otherwise, they are automatically chosen by
                      Confluent Cloud.
                    items:
                      type: string
                    type: array
                type: object
              initProvider:
                description: THIS IS A BETA FIELD. It will be honored unless the Management
                  Policies feature flag is disabled. InitProvider holds the same fields
                  as ForProvider, with the exception of Identifier and other resource
                  reference fields. The fields that are in InitProvider are merged
                  into ForProvider when the resource is created. The same fields are
                  also added to the terraform ignore_changes hook, to avoid updating
                  them after creation. This is useful for fields that are required
                  on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them,
                  like an autoscaler.
                properties:
                  aws:
                    description: 'The AWS-specific network details if available. It
                      supports the following:'
                    items:
                      type: object
                    type: array
                  azure:
                    description: 'The Azure-specific network details if available.
                      It supports the following:'
                    items:
                      type: object
                    type: array
                  cidr:
                    description: The IPv4 CIDR block to be used for the network. Must
                      be /16. Required for VPC peering and AWS TransitGateway. The
                      IPv4 CIDR block to used for this network. Must be /16. Required
                      for VPC peering and AWS TransitGateway.
                    type: string
                  cloud:
                    description: 'The cloud service provider in which the network
                      exists. Accepted values are: AWS, AZURE, and GCP. The cloud
                      service provider in which the network exists.'
                    type: string
                  connectionTypes:
                    description: 'The list of connection types that may be used with
                      the network. Accepted connection types are: PEERING, TRANSITGATEWAY,
                      and PRIVATELINK.'
                    items:
                      type: string
                    type: array
                  displayName:
                    description: The name of the Network. The name of the Network.
                    type: string
                  dnsConfig:
                    description: 'Network DNS config. It applies only to the PRIVATELINK
                      network connection type. When omitted, resolution defaults to
                      CHASED_PRIVATE. It supports the following: Network DNS config.
                      It applies only to the PRIVATELINK network connection type.'
                    items:
                      properties:
                        resolution:
                          description: Network DNS resolution. When resolution is
                            CHASED_PRIVATE, clusters in this network require both
                            public and private DNS to resolve cluster endpoints. When
                            resolution is PRIVATE, clusters in this network only require
                            private DNS to resolve cluster endpoints. Network DNS
                            resolution.
                          type: string
                      type: object
                    type: array
                  environment:
                    description: 'supports the following: Environment objects represent
                      an isolated namespace for your Confluent resources for organizational
                      purposes.'
                    items:
                      type: object
                    type: array
                  gcp:
                    description: 'The GCP-specific network details if available. It
                      supports the following:'
                    items:
                      type: object
                    type: array
                  region:
                    description: The cloud provider region where the network exists.
                      The cloud service provider region where the network exists.
                    type: string
                  reservedCidr:
                    description: The reserved IPv4 CIDR block to be used for the network.
                      Must be /24. If not specified, Confluent Cloud Network uses
                      172.20.255.0/24. The IPv4 CIDR block reserved for Confluent
                      Cloud Network. Must be /24. If not specified, Confluent Cloud
                      Network uses 172.20.255.0/24
                    type: string
                  zoneInfo:
                    description: 'Each item represents information related to a single
                      zone. It supports the following: Each item represents information
                      related to a single zone.'
                    items:
                      properties:
                        cidr:
                          description: The IPv4 CIDR block to be used for the network.
                            Must be /16. Required for VPC peering and AWS TransitGateway.
                          type: string
                        zoneId:
                          description: Cloud provider zone ID.
                          type: string
                      type: object
                    type: array
                  zones:
                    description: The 3 availability zones for this network. They can
                      optionally be specified for AWS networks used with PrivateLink,
                      for GCP networks used with Private Service Connect, and for
                      AWS and GCP networks used with Peering. Otherwise, they are
                      automatically chosen by Confluent Cloud. On AWS, zones are AWS
                      AZ IDs, for example, use1-az3. On GCP, zones are GCP zones,
                      for example, us-central1-c. On Azure, zones are Confluent-chosen
                      names (for example, 1, 2, 3) since Azure does not have universal
                      zone identifiers. The 3 availability zones for this network.
                      They can optionally be specified for only AWS networks used
                      with PrivateLink. Otherwise, they are automatically chosen by
                      Confluent Cloud.
                    items:
                      type: string
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: 'THIS IS A BETA FIELD. It is on by default but can be
                  opted out through a Crossplane feature flag. ManagementPolicies
                  specify the array of actions Crossplane is allowed to take on the
                  managed and external resources. This field is planned to replace
                  the DeletionPolicy field in a future release. Currently, both could
                  be set independently and non-default values would be honored if
                  the feature flag is enabled. If both are custom, the DeletionPolicy
                  field will be ignored. See the design doc for more information:
                  https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md'
                items:
                  description: A ManagementAction represents an action that the Crossplane
                    controllers can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.cloud is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cloud)
                || (has(self.initProvider) && has(self.initProvider.cloud))'
            - message: spec.forProvider.connectionTypes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.connectionTypes)
                || (has(self.initProvider) && has(self.initProvider.connectionTypes))'
            - message: spec.forProvider.environment is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.environment)
                || (has(self.initProvider) && has(self.initProvider.environment))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: NetworkStatus defines the observed state of Network.
            properties:
              atProvider:
                properties:
                  aws:
                    description: 'The AWS-specific network details if available. It
                      supports the following:'
                    items:
                      properties:
                        account:
                          description: The AWS account ID associated with the Confluent
                            Cloud VPC. The AWS account ID associated with the Confluent
                            Cloud VPC.
                          type: string
                        privateLinkEndpointService:
                          description: The endpoint service of the Confluent Cloud
                            VPC (used for PrivateLink) if available. The endpoint
                            service of the Confluent Cloud VPC (used for PrivateLink)
                            if available.
                          type: string
                        vpc:
                          description: The Confluent Cloud VPC ID. The Confluent Cloud
                            VPC ID.
                          type: string
                      type: object
                    type: array
                  azure:
                    description: 'The Azure-specific network details if available.
                      It supports the following:'
                    items:
                      properties:
                        privateLinkServiceAliases:
                          additionalProperties:
                            type: string
                          description: The mapping of zones to Private Link Service
                            Aliases if available. Keys are zone names, for example,
                            1 and values are Azure Private Link Service Aliases, for
                            example, s-nk99e-privatelink-1.8c43dcd0-695c-1234-bc35-11fe6abb303a.centralus.azure.privatelinkservice.
                          type: object
                      type: object
                    type: array
                  cidr:
                    description: The IPv4 CIDR block to be used for the network. Must
                      be /16. Required for VPC peering and AWS TransitGateway. The
                      IPv4 CIDR block to used for this network. Must be /16. Required
                      for VPC peering and AWS TransitGateway.
                    type: string
                  cloud:
                    description: 'The cloud service provider in which the network
                      exists. Accepted values are: AWS, AZURE, and GCP. The cloud
                      service provider in which the network exists.'
                    type: string
                  connectionTypes:
                    description: 'The list of connection types that may be used with
                      the network. Accepted connection types are: PEERING, TRANSITGATEWAY,
                      and PRIVATELINK.'
                    items:
                      type: string
                    type: array
                  displayName:
                    description: The name of the Network. The name of the Network.
                    type: string
                  dnsConfig:
                    description: 'Network DNS config. It applies only to the PRIVATELINK
                      network connection type. When omitted, resolution defaults to
                      CHASED_PRIVATE. It supports the following: Network DNS config.
                      It applies only to the PRIVATELINK network connection type.'
                    items:
                      properties:
                        resolution:
                          description: Network DNS resolution. When resolution is
                            CHASED_PRIVATE, clusters in this network require both
                            public and private DNS to resolve cluster endpoints. When
                            resolution is PRIVATE, clusters in this network only require
                            private DNS to resolve cluster endpoints. Network DNS
                            resolution.
                          type: string
                      type: object
                    type: array
                  dnsDomain:
                    description: The root DNS domain for the network, for example,
                      pr123a.us-east-2.aws.confluent.cloud if applicable. Present
                      on Networks that support Private Link. The root DNS domain for
                      the network if applicable. Present on networks that support
                      PrivateLink.
                    type: string
                  environment:
                    description: 'supports the following: Environment objects represent
                      an isolated namespace for your Confluent resources for organizational
                      purposes.'
                    items:
                      properties:
                        id:
                          description: The ID of the Environment that the Network
                            belongs to, for example, env-abc123. The unique identifier
                            for the environment.
                          type: string
                      type: object
                    type: array
                  gcp:
                    description: 'The GCP-specific network details if available. It
                      supports the following:'
                    items:
                      properties:
                        privateServiceConnectServiceAttachments:
                          additionalProperties:
                            type: string
                          description: The mapping of zones to Private Service Connect
                            Service Attachments if available. Keys are zones and values
                            are GCP Private Service Connect service attachment. The
                            mapping of zones to Private Service Connect service attachments
                            if available. Keys are zones and values are [GCP Private
                            Service Connect service attachment](https://cloud.google.com/vpc/docs/configure-private-service-connect-producer#api_7).
                          type: object
                        project:
                          description: The GCP Project ID associated with the Confluent
                            Cloud VPC. The GCP project.
                          type: string
                        vpcNetwork:
                          description: The network name of the Confluent Cloud VPC.
                            The GCP VPC network name.
                          type: string
                      type: object
                    type: array
                  id:
                    description: The ID of the Network, for example, n-abc123.
                    type: string
                  region:
                    description: The cloud provider region where the network exists.
                      The cloud service provider region where the network exists.
                    type: string
                  reservedCidr:
                    description: The reserved IPv4 CIDR block to be used for the network.
                      Must be /24. If not specified, Confluent Cloud Network uses
                      172.20.255.0/24. The IPv4 CIDR block reserved for Confluent
                      Cloud Network. Must be /24. If not specified, Confluent Cloud
                      Network uses 172.20.255.0/24
                    type: string
                  resourceName:
                    description: The Confluent Resource Name of the Network. The Confluent
                      Resource Name of the Network.
                    type: string
                  zonalSubdomains:
                    additionalProperties:
                      type: string
                    description: The DNS subdomain for each zone. Present on networks
                      that support Private Link. Keys are zone names, for example,
                      use2-az1 and values are DNS domains, for example, use2-az1.pr123a.us-east-2.aws.confluent.cloud.
                      The DNS subdomain for each zone. Present on networks that support
                      PrivateLink. Keys are zones and values are DNS domains.
                    type: object
                  zoneInfo:
                    description: 'Each item represents information related to a single
                      zone. It supports the following: Each item represents information
                      related to a single zone.'
                    items:
                      properties:
                        cidr:
                          description: The IPv4 CIDR block to be used for the network.
                            Must be /16. Required for VPC peering and AWS TransitGateway.
                          type: string
                        zoneId:
                          description: Cloud provider zone ID.
                          type: string
                      type: object
                    type: array
                  zones:
                    description: The 3 availability zones for this network. They can
                      optionally be specified for AWS networks used with PrivateLink,
                      for GCP networks used with Private Service Connect, and for
                      AWS and GCP networks used with Peering. Otherwise, they are
                      automatically chosen by Confluent Cloud. On AWS, zones are AWS
                      AZ IDs, for example, use1-az3. On GCP, zones are GCP zones,
                      for example, us-central1-c. On Azure, zones are Confluent-chosen
                      names (for example, 1, 2, 3) since Azure does not have universal
                      zone identifiers. The 3 availability zones for this network.
                      They can optionally be specified for only AWS networks used
                      with PrivateLink. Otherwise, they are automatically chosen by
                      Confluent Cloud.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
